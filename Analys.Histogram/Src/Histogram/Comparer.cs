using System;

namespace Analys.Histogram {
  /// <summary>
  /// a.CompareTo(b)
  /// 1: a smaller than b,
  /// 0: a equals to b,
  /// -1:a larger than b
  /// </summary>
  public static class Comparer {

    
  }
}