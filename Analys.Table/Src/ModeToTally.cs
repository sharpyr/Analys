using System;

namespace Analys.Table {
  public static class ModeToTally<T, TK> {
    // public static Func<object, object, object> ModeToAccumulator(Accumulated mode) {
    //   switch (mode) {
    //     case Accumulated.Merge: return Accumulator<T>.Accum;
    //     case Accumulated.Accum:
    //       break;
    //     case Accumulated.Incre: return Accumulator<T>.Incre;
    //     case Accumulated.Count:
    //       break;
    //     case Accumulated.Average:
    //       break;
    //     case Accumulated.Max:
    //       break;
    //     case Accumulated.Min:
    //       break;
    //     case Accumulated.First:
    //       break;
    //     case Accumulated.Last:
    //       break;
    //     default:
    //       throw new ArgumentOutOfRangeException(nameof(mode), mode, null);
    //   }
    // }
  }
}